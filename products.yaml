products:
  - name: test-cmd-tool
    quota: 1000,
    quotaInterval: 1,
    quotaTimeUnit: minute,
    # Optional: how product-level quota counters are applied (Apigee X/Hybrid).
    # Defaults to PRODUCT (one shared counter for all operations on the product).
    # PROXY = shared counter per proxy
    # OPERATION = separate counter per operation
    # quotaCounterScope: PRODUCT
    description: |
      test-cmd-tool2
    environments:
      - test
    proxies:
      - apigee-account-proxy-v1
    # Optional: assign this product to an Apigee Space (Apigee X/Hybrid only).
    # space: red
